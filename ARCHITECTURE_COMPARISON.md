# 🏗️ Architecture Comparison (เปรียบเทียบสถาปัตยกรรม)

## 1. Layered Architecture (Before)
*รูปแบบเดิม: Monolithic (รันทุกอย่างรวมกันในโปรเจกต์เดียว)*

### ✅ Pros (ข้อดี)
- **Simplicity:** เริ่มต้นพัฒนาได้ง่าย โครงสร้างไม่ซับซ้อน โค้ดทั้งหมดอยู่ที่เดียวกัน
- **Performance:** การรับส่งข้อมูลรวดเร็วเพราะเป็นการเรียกฟังก์ชันภายใน (Function Call) ไม่ต้องผ่าน Network
- **Easy Deployment:** ติดตั้งง่ายเพราะมีแค่โปรเจกต์เดียว (Single Deploy)

### ❌ Cons (ข้อเสีย)
- **Tightly Coupled:** Frontend และ Backend ผูกติดกันแน่น แก้ไขจุดหนึ่งอาจกระทบอีกจุด
- **Hard to Scale:** ขยายระบบยาก หากคนเข้าเว็บเยอะต้องเพิ่ม Server ทั้งก้อน (แยกขยายเฉพาะส่วนไม่ได้)
- **Technology Lock-in:** เปลี่ยนเทคโนโลยีฝั่งหน้าบ้านยาก เพราะผูกติดกับ Logic ของหลังบ้าน

---

## 2. Client-Server Architecture (After)
*รูปแบบใหม่: แยก Frontend และ Backend ออกจากกัน (REST API)*

### ✅ Pros (ข้อดี)
- **Separation of Concerns:** แยกหน้าที่ชัดเจน (Frontend=แสดงผล, Backend=ข้อมูล) ทำให้โค้ดเป็นระเบียบดูแลรักษาง่าย
- **Multi-Platform Support:** API ชุดเดียวสามารถรองรับ Client ได้หลากหลาย (Web, Mobile App, Desktop)
- **Scalability:** สามารถขยาย Server (Scale) เฉพาะส่วน Backend ได้เมื่อมีการใช้งานหนัก
- **Parallel Development:** ทีม Frontend และ Backend สามารถทำงานไปพร้อมๆ กันได้โดยไม่ทับซ้อน

### ❌ Cons (ข้อเสีย)
- **Complexity:** มีความซับซ้อนเพิ่มขึ้น ต้องจัดการเรื่อง Network, HTTP Status และ API Endpoints
- **CORS Issues:** ต้องจัดการเรื่องสิทธิ์การเข้าถึงข้ามโดเมน (Cross-Origin Resource Sharing)
- **Network Latency:** มีความหน่วงเวลาเล็กน้อยจากการส่งข้อมูลผ่านเครือข่าย

---

## 🛠️ Changes Made (สิ่งที่เปลี่ยนแปลงในโปรเจกต์นี้)

### 1. Separation (การแยกส่วนโปรเจกต์)
- **Old:** ไฟล์ HTML/JS ปนอยู่กับ Server Code
- **New:** แยกโฟลเดอร์ชัดเจน
  - `backend/`: รัน Server บน Node.js (Port 3000)
  - `frontend/`: รันหน้าเว็บแยกต่างหาก (HTML/CSS/JS)

### 2. Communication (การสื่อสาร)
- เปลี่ยนจากการ Render HTML จากฝั่ง Server มาเป็นการใช้ **REST API**
- ใช้ **Fetch API** ในฝั่ง Client เพื่อยิง Request (GET, POST, PUT, DELETE) ไปหา Server

### 3. CORS Configuration
- เพิ่ม **CORS Middleware** ในฝั่ง Backend (`src/presentation/middlewares/cors.js`) เพื่ออนุญาตให้ Frontend (Localhost) สามารถเข้าถึง API ได้

### 4. API Response Format
- ปรับปรุงการตอบกลับของ Server ให้เป็นมาตรฐาน **JSON** - ตัวอย่าง Response: `{ "message": "Success", "data": { ... } }` เพื่อให้ Frontend นำไปแสดงผลหรือแจ้งเตือนด้วย SweetAlert2 ได้ง่ายขึ้น